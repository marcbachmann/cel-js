{
  "name": "benchmark",
  "version": "1.0.0",
  "description": "This directory contains comprehensive benchmarks comparing the performance against older runs",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bench-node": "^0.14.0"
  }
}
